{{ form_start(form) }}{# debut du formulaire#}

{{ form_widget (form) }} {# contenu du formulaire#}

<input type='submit' value="{{submitButtonText | default('Create Pin')}}" formnovalidate class="btn btn-primary btn-block">

{{ form_end(form) }}{# fin du formulaire#}
